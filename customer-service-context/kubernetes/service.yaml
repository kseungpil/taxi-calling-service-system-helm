apiVersion: v1
kind: Service
metadata:
  name: customer-service-context
  labels:
    app: customer-service-context
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: customer-service-context