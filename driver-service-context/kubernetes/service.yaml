apiVersion: v1
kind: Service
metadata:
  name: driver-service-context
  labels:
    app: driver-service-context
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: driver-service-context