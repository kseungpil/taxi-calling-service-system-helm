apiVersion: v1
kind: Service
metadata:
  name: calling-service-context
  labels:
    app: calling-service-context
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: calling-service-context